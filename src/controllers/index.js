const auth = require('./auth');
const categories = require('./categories');
const currencies = require('./currencies');
const wishes = require('./wishes');

module.exports = { auth, categories, currencies, wishes };
